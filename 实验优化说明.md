# 实验流程优化说明

**优化时间**：2026-01-10  
**优化内容**：PreGANPlus和PreGANPlusEnhanced共享Transformer编码器

---

## 🎯 优化目标

**问题**：PreGANPlus和PreGANPlusEnhanced都使用Transformer编码器，如果分别训练会重复训练Transformer。

**解决方案**：调整训练顺序，确保Transformer只训练一次。

---

## 📋 编码器共享关系

### 编码器checkpoint文件

| 方法 | 编码器类型 | Checkpoint文件路径 |
|------|-----------|-------------------|
| **PreGAN** | FPE | `checkpoints/simulator_FPE_16.ckpt` |
| **PreGANPlus** | Transformer | `checkpointsplus/simulator_Transformer_16.ckpt` |
| **PreGANPlusEnhanced** | Transformer | `checkpointsplus/simulator_Transformer_16.ckpt` ⭐ |

**关键发现**：
- ✅ PreGANPlus和PreGANPlusEnhanced使用**相同的Transformer编码器**
- ✅ 两者使用**相同的checkpoint文件**：`simulator_Transformer_16.ckpt`
- ✅ 如果先训练PreGANPlus，Transformer会被训练并保存
- ✅ 然后训练PreGANPlusEnhanced时，会直接加载已训练的Transformer，**不重复训练**

---

## ⚙️ 优化后的训练顺序

### 当前训练顺序（已优化）

```bash
# 1. PreGAN：训练FPE编码器 + GAN
PreGAN → FPE编码器训练（如果未训练）→ GAN训练（1200步）

# 2. PreGANPlus：训练Transformer编码器 + GAN
PreGANPlus → Transformer编码器训练（如果未训练）→ GAN训练（1200步）
# Transformer会被保存到 checkpointsplus/simulator_Transformer_16.ckpt

# 3. PreGANPlusEnhanced：使用已训练的Transformer + GAN
PreGANPlusEnhanced → 加载已训练的Transformer（不重复训练）→ GAN训练（1200步）
```

### 工作原理

**代码逻辑**：`recovery/PreGANPlusEnhanced.py`

```python
def load_models(self):
    # Load encoder model (same as PreGANPlus)
    self.model, self.optimizer, self.epoch, self.accuracy_list = \
        load_model(model_plus_folder, f'{self.env_name}_{self.model_name}.ckpt', self.model_name)
    # Train the model if not trained
    if self.epoch == -1: 
        self.train_model()  # 只有epoch == -1时才训练
    # ...
```

**关键点**：
- 如果checkpoint已存在且`epoch >= 0`，直接加载，**不训练**
- 如果checkpoint不存在或`epoch == -1`，才会训练

**因此**：
- 如果先训练PreGANPlus，Transformer会被训练（epoch=49），并保存
- 然后训练PreGANPlusEnhanced时，会检测到checkpoint已存在且epoch=49，直接加载，**不重复训练**

---

## 📊 优化效果

### 节省时间

**优化前**（如果分别训练）：
- PreGANPlus：Transformer训练（50 epochs）+ GAN训练（1200步）
- PreGANPlusEnhanced：Transformer训练（50 epochs）+ GAN训练（1200步）
- **总时间**：2×Transformer训练 + 2×GAN训练

**优化后**（共享Transformer）：
- PreGANPlus：Transformer训练（50 epochs）+ GAN训练（1200步）
- PreGANPlusEnhanced：直接加载Transformer + GAN训练（1200步）
- **总时间**：1×Transformer训练 + 2×GAN训练

**节省**：1×Transformer训练时间（约50 epochs的训练时间）

### 确保一致性

- ✅ 两个方法使用**完全相同的Transformer编码器**
- ✅ 避免因训练差异导致的性能差异
- ✅ 更公平的对比实验

---

## 🔧 实现细节

### 运行脚本优化

**文件**：`scripts/run_paper_experiment.sh`

**优化后的顺序**：
```bash
# 1. PreGAN（FPE编码器）
python3 scripts/paper_experiment_stage3_gan_training.py --method PreGAN
python3 main.py -e "" -m 0

# 2. PreGANPlus（Transformer编码器，会训练并保存）
python3 scripts/paper_experiment_stage3_gan_training.py --method PreGANPlus
python3 main.py -e "" -m 0
# Transformer保存到：checkpointsplus/simulator_Transformer_16.ckpt

# 3. PreGANPlusEnhanced（使用已训练的Transformer）
python3 scripts/paper_experiment_stage3_gan_training.py --method PreGANPlusEnhanced
python3 main.py -e "" -m 0
# 直接加载：checkpointsplus/simulator_Transformer_16.ckpt（不重复训练）
```

### 验证方法

**检查Transformer是否只训练一次**：

```bash
# 查看checkpoint文件的修改时间
ls -lh recovery/PreGANSrc/checkpointsplus/simulator_Transformer_16.ckpt

# 查看训练日志
grep "Creating new model\|Loading pre-trained model" \
    experiment_logs/paper_experiment/stage2+3_training_*.log
```

**预期结果**：
- PreGANPlus日志：`Creating new model: Transformer_16` 或 `Loading pre-trained model: Transformer_16`
- PreGANPlusEnhanced日志：`Loading pre-trained model: Transformer_16`（不会创建新模型）

---

## 📝 注意事项

### 1. 训练顺序很重要

**必须**先训练PreGANPlus，再训练PreGANPlusEnhanced：
- 如果先训练PreGANPlusEnhanced，Transformer会被训练
- 然后训练PreGANPlus时，会使用已训练的Transformer（这是可以的，但顺序不直观）

**推荐顺序**：PreGAN → PreGANPlus → PreGANPlusEnhanced

### 2. 如果Transformer已存在

**如果checkpoint已存在**（例如从之前的实验）：
- PreGANPlus会直接加载已训练的Transformer（不重复训练）
- PreGANPlusEnhanced也会直接加载已训练的Transformer（不重复训练）

**这是正常的**，不会影响实验结果。

### 3. 重新训练Transformer

**如果需要重新训练Transformer**：
```bash
# 删除checkpoint文件
rm recovery/PreGANSrc/checkpointsplus/simulator_Transformer_16.ckpt

# 然后运行训练，Transformer会被重新训练
```

---

## ✅ 优化完成

**优化效果**：
- ✅ Transformer只训练一次（在PreGANPlus阶段）
- ✅ PreGANPlusEnhanced直接使用已训练的Transformer
- ✅ 节省训练时间
- ✅ 确保两个方法使用相同的Transformer编码器

**当前配置**：
- 训练顺序：PreGAN → PreGANPlus → PreGANPlusEnhanced
- Transformer共享：PreGANPlus和PreGANPlusEnhanced共享同一个checkpoint

---

**优化完成时间**：2026-01-10  
**维护者**：实验团队
