# PreGANPlus 实验运行说明

**最后更新**：2026-01-10  
**实验流程版本**：阶段2+3合并版本

---

## 📋 实验流程概览

```
阶段1: 数据收集 (1000步)
   ↓
阶段2+3: 编码器训练（自动）+ GAN训练 (1200步)
   - PreGAN/PreGANPlus/PreGANPlusEnhanced
   ↓
阶段2: CMODLB编码器训练 (1200步)
   - CMODLB（不需要GAN训练）
   ↓
阶段4: 测试评估 (100步)
   - 所有方法对比
```

---

## 🚀 快速开始

### 1. 环境准备

```bash
# 切换到用户账户
su - user

# 激活conda环境
conda activate pregan

# 进入项目目录
cd /home/user/workspace/PreGANPlus
```

### 2. 一键运行完整实验

```bash
# 运行完整论文实验流程
bash scripts/run_paper_experiment.sh
```

**实验流程**：
1. **阶段1**：数据收集（1000步）
2. **阶段2+3**：编码器训练 + GAN训练（1200步）
   - PreGAN
   - PreGANPlus
   - PreGANPlusEnhanced
3. **阶段2**：CMODLB编码器训练（1200步）
4. **阶段4**：测试评估（100步，所有方法对比）

---

## 📝 各阶段详细说明

### 阶段1：数据收集

**目的**：收集1000步数据用于编码器训练

**配置**：
- `NUM_SIM_STEPS = 1000`
- `recovery = Recovery()`（无恢复机制）

**运行**：
```bash
python3 scripts/paper_experiment_stage1_data_collection.py
python3 main.py -e "" -m 0
```

**输出**：
- 数据文件：`logs/RPiEdge_BWGD2_1000_*/time_series.npy`
- 自动拷贝到：`recovery/PreGANSrc/data/simulator/`

---

### 阶段2+3：编码器训练 + GAN训练（合并）

**目的**：
1. 自动训练编码器（如果未训练）
2. 在线训练GAN（1200步）

**方法**：PreGAN, PreGANPlus, PreGANPlusEnhanced

**配置**：
- `NUM_SIM_STEPS = 1200`
- `training = True`
- 编码器训练使用阶段1的1000步数据（离线训练）

**重要说明**：
- ✅ **PreGAN**：使用FPE编码器（`checkpoints/simulator_FPE_16.ckpt`）
- ✅ **PreGANPlus**：使用Transformer编码器（`checkpointsplus/simulator_Transformer_16.ckpt`）
- ✅ **PreGANPlusEnhanced**：**共享PreGANPlus的Transformer编码器**（不重复训练）
  - 如果PreGANPlus已训练，PreGANPlusEnhanced会直接加载已训练的Transformer
  - 如果PreGANPlus未训练，PreGANPlusEnhanced会先训练Transformer

**运行**：
```bash
# 单个方法
python3 scripts/paper_experiment_stage3_gan_training.py --method PreGAN
python3 main.py -e "" -m 0

# 或使用一键脚本（运行所有方法）
bash scripts/run_paper_experiment.sh
```

**训练顺序**（优化后）：
1. **PreGAN**：训练FPE编码器 + GAN
2. **PreGANPlus**：训练Transformer编码器 + GAN（Transformer会保存）
3. **PreGANPlusEnhanced**：使用已训练的Transformer + GAN（不重复训练Transformer）

**工作原理**：
1. 初始化时，如果编码器未训练（`epoch == -1`），自动训练
2. 编码器训练完成后，继续运行1200步的GAN训练
3. 每次异常检测都会训练GAN

---

### 阶段2：CMODLB编码器训练

**目的**：训练CMODLB的FCN编码器

**配置**：
- `NUM_SIM_STEPS = 1200`（确保环境稳定）
- `training = False`
- 编码器训练使用阶段1的1000步数据（离线训练）

**运行**：
```bash
python3 scripts/paper_experiment_stage2_encoder_training.py --method CMODLB
python3 main.py -e "" -m 0
```

---

### 阶段4：测试评估

**目的**：测试所有方法，对比性能

**配置**：
- `NUM_SIM_STEPS = 100`
- `training = False`

**方法**：PreGAN, PreGANPlus, PreGANPlusEnhanced, PCFT, DFTM, ECLB, CMODLB

**运行**：
```bash
# 单个方法
python3 scripts/paper_experiment_stage4_testing.py --method PreGAN
python3 main.py -e "" -m 0

# 或使用一键脚本（运行所有方法）
bash scripts/run_paper_experiment.sh
```

---

## ⚙️ PreGANPlusEnhanced 权重配置

**当前配置**（优化能量和响应时间）：

```python
self.energy_weight = 0.40             # 能量优化权重（增强能量优势）
self.response_time_weight = 0.45     # 响应时间约束权重（保持响应时间优势）
self.migration_cost_weight = 0.15    # 迁移成本约束权重（接受更高的迁移次数）
```

**策略说明**：
- ✅ **能量权重0.40**：增强能量优化，保持能量优势
- ✅ **响应时间权重0.45**：保持响应时间优势
- ⚠️ **迁移成本权重0.15**：降低权重，因为迁移次数上不可能占优

**调整位置**：`recovery/PreGANPlusEnhanced.py` 第30-32行

---

## 📊 实验结果位置

**日志文件**：
- `experiment_logs/paper_experiment/stage1_data_collection_*.log`
- `experiment_logs/paper_experiment/stage2+3_training_*.log`
- `experiment_logs/paper_experiment/stage2_encoder_training_CMODLB_*.log`
- `experiment_logs/paper_experiment/stage4_testing_*.log`

**数据文件**：
- `logs/RPiEdge_BWGD2_*/` - 各阶段的运行数据

**模型文件**：
- `recovery/PreGANSrc/checkpoints/` - PreGAN模型
- `recovery/PreGANSrc/checkpointsplus/` - PreGANPlus/PreGANPlusEnhanced模型
- `recovery/CMODLBSrc/checkpoints/` - CMODLB模型

---

## 🔧 常见问题

### Q1: 编码器训练需要多长时间？

**A**: 编码器训练是自动的，使用离线数据：
- FPE/Transformer：50个epochs
- FCN：30个epochs
- 训练时间取决于数据量和硬件性能

### Q2: 如果训练被中断怎么办？

**A**: 
- 编码器训练：可以从checkpoint继续（如果epoch < 49/29）
- GAN训练：可以从checkpoint继续（如果epoch < 1200）

### Q3: 如何只运行某个阶段？

**A**: 直接运行对应的脚本：
```bash
# 只运行阶段1
python3 scripts/paper_experiment_stage1_data_collection.py
python3 main.py -e "" -m 0

# 只运行阶段2+3（某个方法）
python3 scripts/paper_experiment_stage3_gan_training.py --method PreGAN
python3 main.py -e "" -m 0

# 只运行阶段4（某个方法）
python3 scripts/paper_experiment_stage4_testing.py --method PreGAN
python3 main.py -e "" -m 0
```

### Q4: 如何调整PreGANPlusEnhanced的权重？

**A**: 编辑 `recovery/PreGANPlusEnhanced.py` 第30-32行：
```python
self.energy_weight = 0.40             # 调整能量权重
self.response_time_weight = 0.45      # 调整响应时间权重
self.migration_cost_weight = 0.15     # 调整迁移成本权重
```

**注意**：三个权重之和应该接近1.0（允许小幅偏差）

---

## 📚 参考文档

- **实验参数配置**：`paper_experiment.md`
- **代码实现**：`recovery/PreGANPlusEnhanced.py`
- **脚本说明**：`scripts/` 目录下的各脚本文件

---

**最后更新**：2026-01-10  
**维护者**：实验团队
