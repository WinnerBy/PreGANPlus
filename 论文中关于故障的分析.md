这份文档旨在系统性地梳理 **PreGANPlus** 框架中的故障预测机制，重点分析**真实环境与仿真环境的差异**，并针对仿真环境下故障检测准确率低（<20%）的问题提供深度的根因分析与改进方案。

---

# PreGANPlus 框架：故障预测机制与仿真环境优化分析文档

## 1. 故障定义与基准 (Ground Truth Definitions)

在 PreGANPlus 中，故障并非随机标记，而是基于严格的系统资源指标阈值定义的。这是所有训练和评估的基础。

### 1.1 故障触发条件 (Fault Conditions)
基于 **ADE (Anomaly Detection Engine)** 的定义，系统监控以下指标，且需**持续 60秒**满足条件才被判定为故障：

| 故障类型 | 缩写 | 触发阈值/条件 | 物理含义 |
| :--- | :--- | :--- | :--- |
| **CPU 过载** | **CPUO** | CPU 利用率 > **90%** | 计算资源耗尽，任务处理延迟 |
| **异常内存分配** | **AMA** | RAM I/O 利用率 > **90%** | 内存带宽饱和，通常伴随 Swap 频繁 |
| **内存泄漏** | **MEL** | 内存分配速率 > **1MB/3s** | 应用程序存在 Bug，导致内存持续上涨 |
| **网络过载** | **NOL** | 网络缓冲区溢出 | 带宽不足或延迟过高导致数据堆积 |
| **异常磁盘利用** | **ADU** | 磁盘读写被节流 (Throttled) | I/O 操作超过硬件限制 |

### 1.2 故障标签分类 (Fault Labels)
为了模型训练，上述 5 种物理现象被归纳为 3 类标签：
*   **Class 1: CPU Fault** (源自 CPUO)
*   **Class 2: Network Fault** (源自 NOL + ADU)
*   **Class 3: RAM Fault** (源自 AMA + MEL)
*   **Class 0: Normal** (无故障)

---

## 2. 真实环境 vs. 仿真环境：故障生成机制对比

这是理解“为什么仿真效果差”的关键。两者产生故障的**源头**完全不同。

### 2.1 真实环境 (Physical Testbed)
在真实环境（如树莓派集群）中，故障是**“负载诱发” (Workload-induced)** 的自然物理结果。

*   **机制**：
    1.  **调度器**将高负载容器（如 DeFog 基准测试中的 Yolo/PocketSphinx）分配给节点。
    2.  **容器运行**消耗真实的 CPU 周期和内存页。
    3.  **外部干扰**（如 `NetLimiter`）根据 SUMO 轨迹限制物理带宽。
    4.  **检测**：`docker stats` 读取实时数据 -> 超过阈值 -> 标记为故障。
*   **数据特征**：数据充满噪声（Noise），具有突发性尖峰（Spikes），且具有物理连锁反应（如内存满导致 CPU 颠簸）。

### 2.2 仿真环境 (Coupled-Simulation)
在仿真环境中，没有真实的容器运行。故障是**“模型预测” (Model-predicted)** 的数值结果。

*   **机制**：
    1.  **调度器**做出决策。
    2.  **代理模型 (Surrogate Model)**（通常是 MLP/LSTM 回归模型）接收当前状态 + 决策，预测下一时刻的 CPU/RAM **数值**。
    3.  **检测**：将预测出的数值输入 ADE 逻辑 -> 超过阈值 -> 标记为故障。
*   **数据特征**：数据平滑（Smooth），趋向于均值，缺乏极端值。

---

## 3. 核心问题分析：为何仿真环境下准确率 < 20%？

在仿真中，故障检测准确率低，**不是因为预测模型（PreGAN）差，而是因为仿真环境（Surrogate）根本没有生成足够的“故障样本”。**

### 3.1 回归均值效应 (Regression to the Mean)
神经网络回归模型（Surrogate）在训练时通常使用 MSE（均方误差）作为损失函数。
*   **现象**：为了最小化整体误差，模型倾向于预测“平均值”。
*   **后果**：
    *   真实 CPU：在 40% 和 95% 之间跳变。
    *   预测 CPU：倾向于输出 70% - 85% 之间的平滑曲线。
    *   **判定失效**：由于阈值是硬性的 **90%**，预测出的 85% 被判定为“正常”。**真正的故障被“平滑”掉了。**

### 3.2 缺乏时序累积效应
故障定义要求“持续 60秒”。
*   **现象**：仿真模型可能偶尔预测出一个 91% 的点，但很难连续 60 个时间步都预测出 >90% 的值（除非模型过拟合）。
*   **后果**：只要中间有一个时间步预测回落到 89%，故障计时器就会重置，导致故障漏报。

---

## 4. 仿真代码改进与复现策略

要在仿真中复现真实环境的故障率，必须在代码层面打破“平滑效应”。

### 策略 A：基于残差的噪声注入 (Residual-based Noise Injection)
不要直接信任 Surrogate 模型的原始输出，需叠加符合真实数据分布的噪声。

*   **代码修改位置**：`framework/interface/SimEnv.py` 中的 `get_state()` 或 `step()`。
*   **实现逻辑**：
    ```python
    # 1. 获取基础预测值
    pred_metrics = surrogate_model.predict(current_state, decision)
    
    # 2. 计算动态噪声 (负载越高，波动越大)
    # sigma 可以通过分析真实数据集的残差分布获得
    sigma = pred_metrics * 0.15 
    noise = np.random.normal(0, sigma)
    
    # 3. 叠加噪声并截断
    final_metrics = np.clip(pred_metrics + noise, 0, 1.0)
    ```

### 策略 B：概率性故障放大 (Probabilistic Fault Amplification)
人为地“推”一把，模拟系统崩溃时的非线性特征。

*   **实现逻辑**：
    ```python
    cpu_pred = pred_metrics['cpu']
    
    # 设定一个“危险区”，例如 80%
    if cpu_pred > 0.80:
        # 以一定概率强制进入故障区 (>90%)
        # 这个概率 P 可以根据真实数据中 [80%-90%] 区间跃迁到 >90% 的频率来设定
        if random.random() < 0.35: 
            cpu_pred = random.uniform(0.91, 0.99)
            
    # 更新 metrics
    pred_metrics['cpu'] = cpu_pred
    ```

### 策略 C：混合建模 (Hybrid Modeling) - 推荐
将“数值预测”与“状态分类”解耦。

1.  **训练一个分类器 (Classifier)**：专门预测下一时刻是否会发生故障（0/1）。由于分类器关注的是边界，它比回归模型更容易捕捉故障。
2.  **仿真逻辑**：
    *   先运行分类器。
    *   如果分类器说 `Fault=True` -> 强制将输出指标设为随机的高值（如 95%~100%）。
    *   如果分类器说 `Fault=False` -> 使用回归模型的预测值。

### 策略 D：调整 Surrogate 模型的损失函数
如果必须重新训练 Surrogate 模型，不要只用 MSE。

*   **加权损失 (Weighted Loss)**：对高负载样本（Label > 0.8）赋予更高的权重。
    $$L = \sum (y_{true} - y_{pred})^2 \times w$$
    当 $y_{true} > 0.9$ 时，设 $w=10$，否则 $w=1$。这将迫使模型学会“敢于预测高值”。

## 5. 总结

在 PreGANPlus 的复现中，**仿真环境过于理想化**是性能差异的根源。

*   **现状**：仿真器生成的指标过于平滑，无法触发 ADE 的阈值，导致故障样本极少，PreGAN 模型无故障可测。
*   **行动**：请优先尝试 **策略 B (概率性故障放大)**，这是修改代码成本最低且效果最显著的方法。通过人工引入“险情”，让仿真环境的数据分布更接近真实世界的恶劣情况。